services:
  - type: web
    name: your-app-name
    runtime: python
    env: python
    region: oregon
    plan: free
    buildCommand: pip install setuptools wheel && pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9